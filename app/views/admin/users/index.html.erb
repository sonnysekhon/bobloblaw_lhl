<main>
  <div class="container">
    <h3>Admin User Management Page</h3>
    <%= paginate @users %>
    <% @users.each do |user| %>
      <div class = "user">
      <%= user.firstname  %> | <%= user.lastname %> | <%= user.email %> | <%= link_to "Edit" , edit_admin_user_path(user) %> | <%= link_to "Delete", admin_user_path(user), method: :delete %> 
      </div>
    <% end %>
    <br>
    <%= link_to "Create User", new_admin_user_path %>
  </div>
</main>